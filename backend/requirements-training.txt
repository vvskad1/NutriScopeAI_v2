# LLM Training Requirements (Heavy Dependencies)
# Install these only if you plan to train models locally

torch>=2.0.0
transformers>=4.30.0
datasets>=2.14.0
tokenizers>=0.13.3
accelerate>=0.20.0
sentencepiece>=0.1.99

# Training optimization
optimum>=1.12.0

# GPU acceleration (optional but recommended)
# Uncomment if you have CUDA GPU:
# torch-audio
# torchaudio
# torchvision

# Evaluation metrics
evaluate>=0.4.0
rouge-score>=0.1.2
bleu>=0.3.0

# Memory optimization
bitsandbytes>=0.39.0

# Distributed training (if needed)
deepspeed>=0.9.5

# Weights & Biases for experiment tracking (optional)
# wandb>=0.15.0

# Jupyter for experimentation (optional)
# jupyter>=1.0.0
# ipykernel>=6.25.0